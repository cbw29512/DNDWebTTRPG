# Room Loading Contract

A prepared Room loads with one DM action. The broad Location stays active, the previous immediate Room is replaced, and the cards assigned to the new Room are prepared automatically. Hidden content remains unrevealed until its reveal rule or trigger fires. The runtime records the active scene, Location, Room, history, and discovered scenes without modifying the source adventure manifest.
