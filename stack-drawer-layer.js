function positionStackDrawers(){document.querySelectorAll('.card-stack.expanded').forEach(stack=>{const toggle=stack.querySelector('.stack-toggle');const drawer=stack.querySelector('.stack-drawer');if(!toggle||!drawer)return;const rect=toggle.getBoundingClientRect();const margin=12;const availableRight=Math.max(240,window.innerWidth-margin-Math.max(margin,rect.left));const desired=Math.min(900,Math.max(320,drawer.scrollWidth||320),availableRight);let left=Math.max(margin,Math.min(rect.left+(rect.width/2)-(desired/2),window.innerWidth-desired-margin));let top=rect.bottom+10;const estimated=Math.min(drawer.scrollHeight||420,window.innerHeight*.68);if(top+estimated>window.innerHeight-margin&&rect.top-estimated-10>margin)top=rect.top-estimated-10;drawer.style.setProperty('--stack-drawer-left',`${Math.round(left)}px`);drawer.style.setProperty('--stack-drawer-top',`${Math.round(Math.max(margin,top))}px`);drawer.style.setProperty('--stack-drawer-width',`${Math.round(desired)}px`);});}
let frame=0;function schedule(){cancelAnimationFrame(frame);frame=requestAnimationFrame(positionStackDrawers);}new MutationObserver(schedule).observe(document.documentElement,{childList:true,subtree:true});window.addEventListener('resize',schedule);window.addEventListener('scroll',schedule,true);document.addEventListener('click',schedule,true);schedule();